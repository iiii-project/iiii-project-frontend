# 生產環境用(搭配 compose.prod.yaml)。複製成 .env 後把下面換成真實值。
# 這些是 docker compose 用來做變數替換的值,跟 backend 的 .env(用 env_file 注入
# 進 backend 容器)是兩份不同的檔案,不要搞混。

# 你的正式網域,DNS A 記錄要先指到這台 VM 的公開 IP,Caddy 才申請得到憑證。
DOMAIN=fortune.example.org
