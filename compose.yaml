services:
  frontend:
    build: .
    ports:
      - "${FRONTEND_PORT:-8080}:80"
    environment:
      BACKEND_URL: "${BACKEND_URL:-http://iiibackend.dev-serve.me}"
      NGINX_ENVSUBST_FILTER: BACKEND_URL
    restart: unless-stopped
